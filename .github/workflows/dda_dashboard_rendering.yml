on:
 # schedule:
  #  - cron:  "*/5 * * * *"
  workflow_dispatch:

name: 01_rendering

jobs:
  run-r-script:
    runs-on: ubuntu-latest
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::tidyverse
            any::magrittr
            any::plotly
            any::janitor
            any::jsonlite
            any::httr
            any::feather

      - name: Render Quarto Project
        uses: quarto-dev/quarto-actions/render@v2
        with:
          to: html # If set, it will be equivalent to `quarto render --to html`
<<<<<<< HEAD
          path: "Development/Data Retrieval/Dead Dogs Analytics. DT - Real Time & Historical Data (with playlists - dev) - gha.qmd"
=======
          path: "Development/Data Retrieval (DT)/Dead Dogs Analytics. DT - Real Time & Historical Data - gha.qmd"
>>>>>>> 8a94a1021f4e0e91e941c5a1bd8ea6d94cbb921c
